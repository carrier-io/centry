The application has been published on the root URL of the main ingress. If it does not work, as a workaround, try to set up NodePort for traefik and connect to it directly (see the values.yaml for the traefik config).